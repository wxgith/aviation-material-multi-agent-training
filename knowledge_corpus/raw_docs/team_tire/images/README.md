# 图像目录

- `SEM/`：扫描电子显微镜原始图像及独立标注图；
- `optical/`：光学显微镜、体视显微镜和宏观磨痕图；
- `damage_cases/`：裂纹、剥落、异常磨耗等教学案例图。

每张图必须在 `data/损伤图像与案例标注.xlsx` 的“图像索引”中登记，并关联 `sample_id`、`source_record_id`、倍率/标尺、相对路径和 SHA-256。
