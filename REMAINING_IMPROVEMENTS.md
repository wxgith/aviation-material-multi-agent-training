# 剩余问题与后续打磨路线

本文记录当前稳定版本之后仍值得处理的事项，避免把“已实现的比赛原型”和“生产级系统”混为一谈。优先级会随比赛提交日期和评审规则调整。

## P0：提交与安全前必须完成

| 事项 | 当前状态 | 完成标准 |
| --- | --- | --- |
| 撤销已暴露的模型密钥 | 密钥曾在聊天中出现 | 旧 Key 失效；新 Key 仅存本机 `.env`，设置限额并不进入提交包 |
| 来源与授权终审 | 已有来源登记和团队授权说明 | 逐项确认公开资料再分发许可、图片引用、团队数据授权和脱敏状态 |
| 正式身份材料 | 方案、PPT 封面仍有人工字段 | 学校、团队、申报人、指导教师和报名表填写并复核 |
| 正式视频 | 讲稿和彩排清单已完成 | MP4 小于 10 分钟，画面、音频、隐私和密钥遮挡检查通过 |
| 最终包完整性 | 候选包已校验 | 加入视频与报名表后重新打包，校验 SHA-256，并在另一台机器抽查解压 |

## P1：高价值工程打磨

| 事项 | 当前不足 | 建议动作 |
| --- | --- | --- |
| 干净环境 Compose 复现 | 已完成“本机应用 + 独立空白 MySQL/ES 数据栈”复现；全容器镜像构建仍受 Docker Hub 网络阻塞 | 已验证 seed、1,766 条索引、三组闭环和 SQL 写入；网络稳定后补全前后端镜像构建 |
| 数据库版本迁移 | 已完成 Alembic `20260812_0001 -> 0002`，兼容旧库基线登记 | 后续每次表结构变更新增 revision，并在备份后演练升级/回滚 |
| 任务状态持久化 | 已将 task/event/result/request 写入 MySQL | 当前支持完成结果恢复和中断任务重试；真正跨进程断点续算仍需队列系统与幂等 Agent |
| 权限与访问控制 | 当前面向本地演示，无登录与角色权限 | 增加教师/学习者角色、会话鉴权、API 限流、TLS 和审计日志 |
| 备份恢复 | 已实现 MySQL dump、ES NDJSON、SHA-256 清单和隔离恢复验证 | 提交前定期执行，并将备份保存到不随源码提交的加密介质 |
| 运行可观测性 | 有 health 和生成审计，缺少集中指标 | 增加结构化日志、请求 ID、错误率、延迟分位数和组件告警 |

## P2：专业效果与证据质量

| 事项 | 当前不足 | 建议动作 |
| --- | --- | --- |
| 三领域均衡性 | 已生成覆盖差距报告；轮胎有 6 个团队实验来源，刹车和复材仍为 0 | 按 `knowledge_corpus/MATERIAL_GAP_ACTION_PLAN.md` 补充授权工况、图像和专家判读样例 |
| 真实 LLM 复核规模 | Qwen 代表性机器回归已通过，但人工抽样规模有限 | 用轮胎、刹车、复材及安全边界各扩充样本，双人复核事实、引用和难度适配 |
| 真正向量检索 | BGE 接口已预留，正式演示实际以 BM25 为主 | 提供本地 BGE 路径，建立向量召回基线，并与 BM25/Hybrid 做同集对照 |
| 知识覆盖边界 | 已建立 3 领域、每领域 8 类能力的库存覆盖矩阵 | 持续补缺并保持“库存代理分不等于专业正确率”的评价边界 |
| 动态生成可重复性 | 外部模型有随机性和服务波动 | 固定提示词版本、低温度、结构校验、回退策略，并保留抽样运行记录 |
| 多模态分析边界 | 当前主要展示已整理图像和证据，不是自动视觉诊断模型 | 答辩中如实表述；后续可增加图像特征提取和专家标注评测集 |

## P3：体验与交付增强

| 事项 | 当前不足 | 建议动作 |
| --- | --- | --- |
| PDF 报告 | 当前直接导出 Markdown | 增加服务端 HTML/PDF 或稳定的浏览器打印模板 |
| 性能与并发 | 已有进度、取消和超时，未做负载测试 | 测试 5/10/20 并发会话、LLM 超时和 ES 慢查询，记录 P50/P95 |
| 可访问性 | 已做桌面/移动视觉检查，未做正式无障碍审计 | 补键盘导航、焦点状态、对比度和屏幕阅读器检查 |
| CI/CD | 当前以本地脚本验收 | 在源码仓库增加后端测试、前端构建、密钥扫描和包校验流水线 |
| 最终 PPT 收敛 | 目录保留多个历史版本 | 选定唯一正式 PPT，历史版本移动到不提交的归档目录 |
| 源码评审入口 | 当前可提交 ZIP，尚无评审仓库链接 | 视赛事要求建立私有仓库并开放只读评审权限 |

## 建议顺序

1. 立即撤销暴露 Key，并完成评委无密钥复现路径。
2. 在第二台机器完成一次全新 Compose 复现，修正所有部署文档偏差。
3. 做三领域知识覆盖差距分析，优先补刹车与复材证据。
4. 扩大真实 LLM 的人工复核样本，但继续保留 mock 为主线兜底。
5. 距提交 2 到 3 周时停止功能扩展，集中完成 PPT、视频、报名表和最终包校验。

## 当前不建议投入

- 从零训练大模型；
- 为追求“智能感”取消规则、证据校验或 fallback；
- 在没有标注集的情况下宣称自动图像诊断准确率；
- 在主演示链路中依赖单一外部模型或公网服务。
