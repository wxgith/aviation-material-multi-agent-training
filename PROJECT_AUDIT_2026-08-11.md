# 项目整体审查与后续完善路线

审查日期：2026-08-11

## 一、总体结论

当前项目已经达到“可运行、可演示、可验证、可回退”的比赛原型状态。主闭环、全局上下文助手、MySQL 持久化、Elasticsearch 检索、知识证据链、专家审核纠偏、学情报告、反馈迭代和提交材料均已形成。当前没有阻断比赛演示的 P0 问题，建议继续保持现有架构并以小步补强为主。

本轮最终验收结果：

- 后端自动化测试：86 passed；
- 前端生产构建：passed；
- Elasticsearch：1,766 条知识切片；
- 核心主流程：50/50；
- 动态追问专项：15/15；
- 全局上下文助手专项：15/15；
- 第二批实验资产测评：20/20；
- 公开文献专项：10/10；
- FAA 维修证据专项：6/6；
- JSON/mock 兜底闭环：passed。

## 二、本轮已完成补强

1. 全局助手新增三层意图路由：界面导航、当前学情状态解释、领域 RAG 问答。
2. 当前得分、薄弱点、强项、学习目标和训练方向走 2-Agent 快速路径，避免无意义等待真实 LLM。
3. 修正“三个训练方向有什么区别”被误判为单一领域问题的情况。
4. 前端助手支持停止任务、失败重试、清空记录和最近 24 小时问答续接。
5. 回答卡直接展示生成模式、知识来源、证据 ID、证据标题和回答边界。
6. 新增 15 条全局助手专项用例及自动 runner，并接入一键验收脚本。
7. 一键启动脚本可在 Docker Engine 未运行时自动启动 Docker Desktop并等待就绪。
8. README、系统设计、API 测试、演示脚本、PPT 文案和冻结说明已同步最新口径。

## 三、仍可完善但不应阻塞冻结的事项

### P1：正式比赛材料一致性

- 重新截取加入全局助手后的首页、处理进度、证据标题和回答边界画面。
- 重新导出正式 PPTX，确保其中的旧测试数字统一更新为 86 passed，并加入“全局助手 15/15”结果。
- 录制一次 9 分钟左右完整彩排，核对页面切换、模型等待和 DBeaver/ES 展示时间。

### P2：真实 LLM 质量扩展

- 当前 Qwen 已完成受控调用验证，但正式演示仍建议先预热并保留 mock fallback。
- 后续可建立覆盖三领域、三画像的真实模型离线回答集，重点复核证据一致性、专业事实、响应时间和回退原因。
- 不建议在答辩前更换模型供应商或大改提示词结构。

### P2：工程长期维护

- 前端 `main.jsx` 约 2,000 行，比赛冻结后可拆分页面、助手和任务组件；冻结前不建议做大规模重构。
- SQL 建表仍使用 `create_all()`，生产部署可升级 Alembic。
- 异步任务状态保存在进程内，生产部署可迁移 Redis/Celery；正式训练会话结果已持久化，不影响当前演示。
- 报告当前以 Markdown 导出，后续可增加服务端 HTML/PDF 模板。

### P3：知识与检索扩展

- 配置本地 BGE 模型后，可对 hybrid 模式补充真实向量召回基线；当前 BM25/规则融合足够支撑稳定演示。
- 继续增加刹车片和复合材料方向的授权实验资料、图像与专家标注，平衡三领域证据密度。
- 所有新增资料继续执行来源登记、授权确认、脱敏、切片清单和专家复核。

## 四、推荐冻结策略

正式演示优先使用 SQL + Elasticsearch + Qwen 可用、mock 自动回退模式。录屏前执行：

```powershell
cd D:\agents
.\scripts\start_project.ps1
.\scripts\project_status.ps1
.\scripts\run_acceptance.ps1
```

若现场外部组件异常，切换 JSON/mock 兜底模式。当前不建议继续引入新的基础设施或修改主 Agent 顺序。

