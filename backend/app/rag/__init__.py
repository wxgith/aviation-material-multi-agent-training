"""Optional RAG helpers.

The main demo does not depend on this package. It supports corpus chunking,
optional embedding, and future indexing pipelines while keeping BM25/JSON
fallback available.
"""
